from setuptools import setup, find_packages

setup(
    name="VPR_methods_evaluation",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add your Python dependencies here
    ],
)